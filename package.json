{
  "name": "stkrz_bot",
  "version": "0.0.0",
  "main": "index.js",
  "author": "Tatiana Fomina",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "install": "cd server && yarn install && cd ../webapp && yarn install",
    "build_webapp": "cd webapp && yarn build",
    "build_server": "cd server && yarn build",
    "start_server": "cd server && node build/index.js",
    "prod": "yarn install && yarn build_webapp && yarn build_server && start_server"
  }
}
