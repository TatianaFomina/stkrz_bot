export enum MiniAppMode {
  /**
   * Default mode. App launched from Telegram chat with bot.
   */
  PM = 'PM',

  /**
   * App launched from any Telegram chat by inline query.
   */
  INLINE = 'INLINE',
}
